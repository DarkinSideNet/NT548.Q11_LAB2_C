apiVersion: v1
kind: Service
metadata:
  name: service-a
  namespace: micro-demo
spec:
  selector:
    app: service-a
  ports:
    - name: http
      port: 80
      targetPort: 3000
  type: ClusterIP
