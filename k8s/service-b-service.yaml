apiVersion: v1
kind: Service
metadata:
  name: service-b
  namespace: micro-demo
spec:
  selector:
    app: service-b
  ports:
    - name: http
      port: 3000
      targetPort: 3000
  type: ClusterIP
