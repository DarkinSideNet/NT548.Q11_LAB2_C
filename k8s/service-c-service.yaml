apiVersion: v1
kind: Service
metadata:
  name: service-c
  namespace: micro-demo
spec:
  selector:
    app: service-c
  ports:
    - name: http
      port: 3000
      targetPort: 3000
  type: ClusterIP
